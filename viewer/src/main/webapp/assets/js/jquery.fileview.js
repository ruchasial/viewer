/**
 * 
 */
 /*var xmlhttp;
	    function init() {
	        // put more code here in case you are concerned about browsers that do not provide XMLHttpRequest object directly
	        xmlhttp = new XMLHttpRequest();
	     }
	     function callResource() {
	         var url = pageContext.request.contextPath + "/webapi/send/view";
	         xmlhttp.open('GET',url,true);
	         xmlhttp.send(null);
	         filepath = xmlhttp.responseText;
	         return filepath;
	               }
	     function viewpath(){
	     	System.out.println(filepath);
	     }



$('#itvs button').click
		( function()
			{$.get ("webapi/send/view",function(data){var path=window.location.href;	
													//window.open(path)
													alert("path is:" +path+ "data is:"+data);}
					);
			}
		)

$.post("webapi/send/uploading",function(data){
        		var path=window.location.href;	
		//window.open(path)
		alert("path is:" +path+ "data is:"+data);});
 }



$('upload').attr('action','webapi/send/uploading')
		*/